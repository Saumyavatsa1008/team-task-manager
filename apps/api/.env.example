NODE_ENV=development
PORT=4000
WEB_ORIGIN=http://localhost:5173
LOG_LEVEL=info

FIREBASE_PROJECT_ID=team-task-management-system

# Local dev: drop serviceAccount.json next to package.json (gitignored).
# Production / Railway: set the base64 env var below.
FIREBASE_SERVICE_ACCOUNT_B64=
